using SD.CharacterSystem;
using UnityEngine;
using UnityEngine.UI;

public class CharacterPortraitPanel : MonoBehaviour
{
    [SerializeField] private Image portrait;

    public void SetCharacter(Character character)
    {
        //Will have to store either a reference to their portrait, or the pieces that make it up
    }
}